import { StrictMode } from "react";
import ReactDOM from "react-dom/client";
import App from "./App.js";
import "./index.css";

ReactDOM(document.getElementById("root")).render(
  <StrictMode>
    <App />
    <h1>Hello, Vite + React + Bootstrap!</h1>
  </StrictMode>
);
